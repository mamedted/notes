//TO MAKE NEW UPDATES
setSelectedFiles((prev) => [...prev, ...Array.from(e.target.files)]);


**USE CONTEXT IS JUST FOR 
Auth
Theme
Language
Global user info

**IN A NUMBER INPUT, IT SHOULD BE type="text" inputMode="numeric"

**TO USE INLINE STYLE style={{color:"red", border:"1px solid red"}}

**WHEN SENDING JSON, use   headers: { "Content-Type": "application/json" },	

**WHEN SENDING FORMDATA, don't use headers



**IF YOU WANT TO TRIGGER THE RE-RENDER OF A COMPONENT IN THE PARENT COMPONENT **
Say, you have a fetch function in the parent container and you want to trigger it
pass the fetch function as props in the child component

<Child onRefresh={fetch} />

Then, in your child component
{onRefresh}

Just trigger the function after clicking a button
onRefresh()